export const defaults=()=>({version:1,profile:{name:'',weight:98,height:185,goal:5,met:3.5},history:[],active:null,loads:{}});
export function elapsed(active,now=Date.now()){return active?active.elapsed+(active.running?Math.max(0,now-active.since):0):0;}
export function calories(ms,weight,met=3.5){return Math.round(ms/60000*met*3.5*weight/200);}
export function weekDays(offset=0,now=new Date()){const start=new Date(now);start.setHours(0,0,0,0);start.setDate(start.getDate()-((start.getDay()+6)%7)+offset*7);return Array.from({length:7},(_,i)=>{const d=new Date(start);d.setDate(d.getDate()+i);return d;});}
export const dateKey=d=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
export function validateBackup(s){if(!s||s.version!==1||!s.profile||!Array.isArray(s.history)||!s.loads||typeof s.loads!=='object')throw Error('Formato de backup inválido.');const p=s.profile;if(typeof p.name!=='string'||p.name.length>60||!Number.isFinite(p.weight)||p.weight<30||p.weight>300||!Number.isFinite(p.height)||p.height<100||p.height>250||!Number.isInteger(p.goal)||p.goal<1||p.goal>7||![3.5,6].includes(p.met))throw Error('Perfil inválido no backup.');for(const h of s.history)if(!h||typeof h.id!=='string'||!['A','B','C'].includes(h.workout)||!Number.isFinite(h.duration)||h.duration<0||!Number.isFinite(h.kcal)||h.kcal<0||!Number.isFinite(Date.parse(h.date)))throw Error('Histórico inválido no backup.');for(const v of Object.values(s.loads))if(typeof v!=='string'||v.length>40)throw Error('Cargas inválidas no backup.');return {...s,active:null};}
